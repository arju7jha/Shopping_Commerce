import 'package:flutter/material.dart';

class ForgetHeading extends StatelessWidget {
  const ForgetHeading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
